# npm configuration for Overleaf project
engine-strict=false
legacy-peer-deps=true
fund=false
audit=false

# Ignore scripts during CI to avoid patch-package issues
ignore-scripts=false

# Use npm registry
registry=https://registry.npmjs.org/
